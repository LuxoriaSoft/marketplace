namespace Luxoria.SDK.Models
{
    /// <summary>
    /// Enum representing the log level (severity).
    /// </summary>
    public enum LogLevel
    {
        Debug,
        Info,
        Warning,
        Error,
        Critical
    }
}
